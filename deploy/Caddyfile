# Replace YOURDOMAIN.com with your real domain (the part after api.).
# DNS: A record `api` → droplet IPv4. Ports 80 and 443 must be open.

api.YOURDOMAIN.com {
	encode gzip

	reverse_proxy 127.0.0.1:3000 {
		flush_interval -1
	}
}
